
import { ArrayType } from '@angular/compiler';

export class ArrayImagen{
        id: Number;
        user_id: String;
        image_id: String;
        sub_id: null;
        created_at: String;
        image: {
          id: String;
          url: String; 
      }
}